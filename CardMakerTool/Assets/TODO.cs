using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TODO : MonoBehaviour
{
    //Custom Window :
        //Call OnInspectorGUI for the ScriptableObject and display it.
        //Load button :  opens file explorer : select a scriptable object that will render in the window
        //Display the data side to side at first
        //Buttons : previous and next : cycles trought file of scriptable objects (put them in an array % length)
        //Button : Go To File :  highlight the scriptableObject in Project window (below)

}
